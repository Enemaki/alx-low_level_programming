#include <stdio.h>
/**
 * main- print lowercase alphabet in reverse
 *
 * Return: always 0
 */

int main(void)
{
	char alph;

	alph = 'z';

	while (alph >= a)
	{
		putchar(alph);
		alph--;
	}
	putchar('\n');

	return (0);
}
